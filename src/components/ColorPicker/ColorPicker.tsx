import React from 'react';
import ToolWrapper from "../ToolWrapper/ToolWrapper";

const ColorPicker = () => {
    return (
        <ToolWrapper>
            ColorPicker
        </ToolWrapper>
    );
};

export default ColorPicker;