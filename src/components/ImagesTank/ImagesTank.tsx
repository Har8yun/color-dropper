import React from 'react';
import ToolWrapper from "../ToolWrapper/ToolWrapper";

const ImagesTank = () => {
    return (
        <ToolWrapper>
            ImagesTank
        </ToolWrapper>
    );
};

export default ImagesTank;